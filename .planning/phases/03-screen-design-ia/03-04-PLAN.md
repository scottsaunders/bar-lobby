---
phase: 03-screen-design-ia
plan: 04
type: execute
wave: 3
depends_on:
  - 03-02
  - 03-03
files_modified:
  - src/renderer/views/news/overview.vue
  - src/renderer/views/profile/[userId].vue
  - src/renderer/views/play/campaign/[campaignId].vue
  - src/renderer/views/library/maps/[id].vue
autonomous: false
requirements:
  - UX-01
user_setup: []

must_haves:
  truths:
    - "News overview has view-title for consistency"
    - "Profile has view-container and view-title"
    - "Deep routes (campaign/[id], maps/[id]) have Breadcrumbs for hierarchy"
    - "All major screens conform to layout spec"
  artifacts:
    - path: src/renderer/views/news/overview.vue
      provides: "News view with view-title"
      contains: "view-title"
    - path: src/renderer/views/profile/[userId].vue
      provides: "Profile view with view-container"
      contains: "view-container"
  key_links:
    - from: "Breadcrumbs.vue"
      to: "campaign/[campaignId].vue, maps/[id].vue"
      via: "view shell import"
      pattern: "Breadcrumbs"
---

<objective>
Complete layout alignment: add view-title to news/overview, add view-container + view-title to profile, integrate Breadcrumbs for deep routes, and verify all screens conform.

Purpose: News and Profile were missing view shell elements; Breadcrumbs improve hierarchy on deep routes.
Output: News and Profile aligned; Breadcrumbs on campaign/[id] and maps/[id]; human verification of layout consistency.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-screen-design-ia/03-RESEARCH.md
@.planning/phases/03-screen-design-ia/03-01-PLAN.md
@src/renderer/components/navbar/Breadcrumbs.vue
@.cursorrules

Breadcrumbs.vue exists but is unused. RESEARCH recommends "planner discretion" for Breadcrumbs on deep routes. NavBar.vue is the likely place to render Breadcrumbs (or within the view shell). profile/[userId].vue has view but no view-container or view-title — add both. news/overview.vue has view-container but no view-title — add minimal view-title (h1 "News" or "Overview").
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add view-title to news/overview and view shell to profile</name>
  <files>src/renderer/views/news/overview.vue, src/renderer/views/profile/[userId].vue</files>
  <action>
1. news/overview.vue: Insert div.view-title with h1 and optional p before .overview-layout. Use t() for "News" or "Overview" — check i18n keys (e.g. lobby.views.news.overview.title). Align view-container padding to canonical (0 xxl sm xxl) if it currently uses md xl.
2. profile/[userId].vue: Wrap content in div.view-container. Add div.view-title with h1 (e.g. user display name or "Profile") and optional p. The existing Panel structure stays inside view-container. Ensure the profile layout (profile-container) remains; view-container provides the outer shell.
  </action>
  <verify>
    <automated>rg "view-title" src/renderer/views/news/overview.vue src/renderer/views/profile/[userId].vue</automated>
  </verify>
  <done>News has view-title; Profile has view-container + view-title</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Breadcrumbs for deep routes</name>
  <files>src/renderer/views/play/campaign/[campaignId].vue, src/renderer/views/library/maps/[id].vue, src/renderer/components/navbar/NavBar.vue</files>
  <action>
1. Determine where Breadcrumbs render: Either in NavBar.vue (when route has meta.hide or is a detail route) or at the top of each deep view. RESEARCH suggests "view shell for routes with meta.hide children". campaign/[campaignId] and maps/[id] have meta.hide: true.
2. Add Breadcrumbs component: If NavBar already has a slot or area for secondary nav, add Breadcrumbs there when current route is a deep route (campaign/:id, maps/:id). Alternatively, add Breadcrumbs inside the view-title area of campaign/[campaignId].vue and maps/[id].vue.
3. Ensure Breadcrumbs receive current route — Breadcrumbs.vue uses useRouter and currentRoute.matched; it should work when placed in a view that has route context. If placing in NavBar, NavBar has router access. If placing in each view, each view has route context.
4. Use the simpler approach: Add Breadcrumbs inside the view shell of campaign/[campaignId].vue and maps/[id].vue, above or beside the view-title, so users can navigate back (e.g. Campaign > Mission Name, or Library > Maps > Map Name).
  </action>
  <verify>
    <automated>rg "Breadcrumbs" src/renderer/views/play/campaign/[campaignId].vue src/renderer/views/library/maps/[id].vue</automated>
  </verify>
  <done>Breadcrumbs visible on campaign detail and map detail screens</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify layout consistency across all screens</name>
  <files>N/A — human verification</files>
  <action>Human verifies layout consistency per how-to-verify below. No code changes.</action>
  <what-built>All major screens (Play, Library, Watch, News, Profile) aligned to canonical view shell; Breadcrumbs on deep routes</what-built>
  <how-to-verify>
1. Start the app (npm run start or equivalent)
2. Navigate through: Play menu → Skirmish, Campaign, Matchmaking, Custom Lobbies, Tournaments
3. Navigate: Watch → Replays
4. Navigate: Library → Maps; select a map to open detail
5. Navigate: News → Overview
6. Navigate to a profile (if possible) or open /profile/:id
7. For each screen: Confirm view-title (h1) is visible; padding is consistent; no visual regressions
8. On campaign/[id] and maps/[id]: Confirm Breadcrumbs show (back + path)
  </how-to-verify>
  <verify>
    <automated>N/A — checkpoint</automated>
  </verify>
  <done>Human confirms all screens conform to layout spec; no regressions</done>
  <resume-signal>Type "approved" or describe any layout issues</resume-signal>
</task>

</tasks>

<verification>
- news/overview has view-title
- profile has view-container + view-title
- Breadcrumbs appear on campaign/[id] and maps/[id]
- Human confirms layout consistency
</verification>

<success_criteria>
- All major screens conform to layout spec
- Navigation hierarchy consistent (Breadcrumbs on deep routes)
- Design system applied across screens
</success_criteria>

<output>
After completion, create `.planning/phases/03-screen-design-ia/03-04-SUMMARY.md`
</output>
