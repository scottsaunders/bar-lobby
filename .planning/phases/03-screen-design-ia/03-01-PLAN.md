---
phase: 03-screen-design-ia
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/styles/_views.scss
  - src/renderer/views/styles/new-styles.vue
autonomous: true
requirements:
  - UX-01
user_setup: []

must_haves:
  truths:
    - "Canonical view shell (view + view-container + view-title) is defined and documented"
    - "Layout spec is visible in Styles tab for reference"
  artifacts:
    - path: src/renderer/styles/_views.scss
      provides: "Base .view-container with canonical padding"
      contains: ".view-container"
    - path: src/renderer/views/styles/new-styles.vue
      provides: "View Shell / Layout & IA documentation section"
      min_lines: 50
  key_links:
    - from: "_views.scss"
      to: "spacing map"
      via: "map.get($spacing, ...)"
      pattern: "map\\.get\\(\\$spacing"
---

<objective>
Define the canonical view shell and layout spec so all screens can align to a single source of truth. Add base .view-container to _views.scss and document the View Shell / Layout & IA in the Styles tab.

Purpose: Phase 3 applies consistent layout across screens; this plan establishes the foundation.
Output: _views.scss with .view-container; Styles tab with View Shell section.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-screen-design-ia/03-RESEARCH.md
@.cursorrules

Key patterns from RESEARCH:
- Canonical padding: `0 xxl sm xxl` (top right bottom left) — used by maps.vue, replays.vue
- Structure: view (flex column) → view-container (flex 1, overflow hidden) → view-title (h1 + optional p) → content
- Exceptions: menu.vue (no view-title, intentional), index.vue (meta.empty, no NavBar)
- Use map.get($spacing, "xxl") etc. — no hard-coded pixels
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add .view-container to _views.scss</name>
  <files>src/renderer/styles/_views.scss</files>
  <action>
Add a base .view-container class to _views.scss with canonical layout and padding:

- display: flex; flex-direction: column; flex: 1; min-height: 0; overflow: hidden; box-sizing: border-box
- padding: 0 map.get($spacing, "xxl") map.get($spacing, "sm") map.get($spacing, "xxl")
- Nested .view-title { padding-left: 0; } — title aligns with content, padding comes from view-container

Reference: library/maps/maps.vue and watch/replays.vue use this pattern. Do NOT change any view files in this task — only add the base class to _views.scss. The class is applied globally via styles.scss @use "views".
  </action>
  <verify>
    <automated>rg "\.view-container" src/renderer/styles/_views.scss -A 8</automated>
  </verify>
  <done>_views.scss contains .view-container with canonical padding using spacing map</done>
</task>

<task type="auto">
  <name>Task 2: Add View Shell section to Styles tab</name>
  <files>src/renderer/views/styles/new-styles.vue</files>
  <action>
Add a "View Shell & Layout" section to the Styles tab (new-styles.vue), placed after Layout Utilities and before or within the design system flow. Include:

1. **Structure:** view → view-container → view-title (h1 + optional p) → content
2. **Canonical padding:** view-container uses 0 xxl sm xxl (top right bottom left)
3. **Exceptions:** menu.vue (no view-title, hero layout); index.vue (meta.empty, no NavBar)
4. **Rules:** All content screens get view-title except menu and index; use design system gap-*, padding-*, flex-row, flex-col; avoid inline styles on view root
5. **Layout patterns:** Two-panel (list + detail), grid of tiles, single-column — reference RESEARCH.md patterns

Use the same markup structure as other sections (flex-col gap-md, h3, design-constants or similar). Follow existing Styles tab conventions. Use typography classes (body-2, caption-1) for descriptions.
  </action>
  <verify>
    <automated>rg "View Shell|view-container|view-title" src/renderer/views/styles/new-styles.vue</automated>
  </verify>
  <done>Styles tab displays View Shell & Layout documentation; developers can reference it</done>
</task>

</tasks>

<verification>
- rg "\.view-container" src/renderer/styles/_views.scss returns match
- rg "View Shell|view-container" src/renderer/views/styles/new-styles.vue returns matches
- npm run build succeeds (or typecheck passes)
</verification>

<success_criteria>
- Canonical view-container exists in _views.scss
- Styles tab has View Shell documentation
- No view files modified in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/03-screen-design-ia/03-01-SUMMARY.md`
</output>
