---
phase: 03-screen-design-ia
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/renderer/views/play/skirmishVsAi.vue
  - src/renderer/views/play/multiplayerLobby.vue
  - src/renderer/views/play/matchmaking.vue
  - src/renderer/views/play/customLobbies.vue
  - src/renderer/views/play/scenarios.vue
  - src/renderer/views/play/tournaments.vue
  - src/renderer/views/play/campaign/index.vue
  - src/renderer/views/play/campaign/[campaignId].vue
  - src/renderer/views/play/campaign/mission.vue
  - src/renderer/views/play/campaign/lore.vue
  - src/renderer/views/play/campaign/tutorial.vue
autonomous: true
requirements:
  - UX-01
user_setup: []

must_haves:
  truths:
    - "Play screens use canonical view shell; no inline styles on view root"
    - "view-container padding is consistent across Play screens"
  artifacts:
    - path: src/renderer/views/play/skirmishVsAi.vue
      provides: "Skirmish view without inline style on view"
      pattern: 'class="view"'
    - path: src/renderer/views/play/multiplayerLobby.vue
      provides: "Multiplayer lobby without inline style on view"
      pattern: 'class="view"'
  key_links:
    - from: "Play views"
      to: "_views.scss .view-container"
      via: "class view-container"
      pattern: "view-container"
---

<objective>
Align all Play screens to the canonical view shell. Remove inline styles from view root; migrate local view-container overrides to use the base from _views.scss where they match; standardize padding.

Purpose: Play flow (menu, skirmish, campaign, matchmaking, customLobbies, tournaments) must have consistent layout.
Output: Play screens conforming to layout spec.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-screen-design-ia/03-RESEARCH.md
@.planning/phases/03-screen-design-ia/03-01-PLAN.md
@src/renderer/styles/_views.scss

Note: menu.vue is an intentional exception (no view-container, no view-title) — do NOT modify menu.vue. .view already has position: relative in _views.scss, so inline style="position: relative" on skirmishVsAi and multiplayerLobby is redundant — remove it.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove inline styles and redundant view-container overrides</name>
  <files>src/renderer/views/play/skirmishVsAi.vue, src/renderer/views/play/multiplayerLobby.vue</files>
  <action>
1. skirmishVsAi.vue: Remove `style="position: relative"` from the root div.view — _views.scss already sets position: relative on .view.
2. multiplayerLobby.vue: Same — remove inline style from root div.view.
3. For both files: If the scoped .view-container block only repeats the canonical padding (0 xxl sm xxl) and .view-title { padding-left: 0 }, remove the entire scoped .view-container block — the base from _views.scss will apply. If the file has layout-specific overrides (e.g. map-preview-container, flex rules), keep only those overrides; remove the padding/view-title parts that duplicate the base.
  </action>
  <verify>
    <automated>rg 'style="position: relative"' src/renderer/views/play/skirmishVsAi.vue src/renderer/views/play/multiplayerLobby.vue 2>/dev/null; test $? -ne 0</automated>
  </verify>
  <done>No inline styles on view root; redundant view-container CSS removed</done>
</task>

<task type="auto">
  <name>Task 2: Standardize view-container across remaining Play screens</name>
  <files>src/renderer/views/play/matchmaking.vue, src/renderer/views/play/customLobbies.vue, src/renderer/views/play/scenarios.vue, src/renderer/views/play/tournaments.vue, src/renderer/views/play/campaign/index.vue, src/renderer/views/play/campaign/[campaignId].vue, src/renderer/views/play/campaign/mission.vue, src/renderer/views/play/campaign/lore.vue, src/renderer/views/play/campaign/tutorial.vue</files>
  <action>
For each file: Inspect the scoped .view-container block. If it defines padding that differs from canonical (0 xxl sm xxl), change it to match. If it only duplicates the base _views.scss .view-container (padding, flex, overflow, .view-title padding-left: 0), remove the duplicate parts — keep only screen-specific overrides (e.g. map-preview-container, custom layout). Use map.get($spacing, "xxl") and map.get($spacing, "sm") for any remaining padding. Ensure each view has: div.view → div.view-container → div.view-title (h1 + optional p) → content. Do NOT change menu.vue.
  </action>
  <verify>
    <automated>rg "view-container|view-title" src/renderer/views/play/matchmaking.vue src/renderer/views/play/customLobbies.vue src/renderer/views/play/scenarios.vue src/renderer/views/play/tournaments.vue src/renderer/views/play/campaign/index.vue --glob "*.vue"</automated>
  </verify>
  <done>All Play screens (except menu) use canonical view-container padding; view-title present</done>
</task>

</tasks>

<verification>
- No inline style="position: relative" on any Play view root
- Play screens have view + view-container + view-title
- view-container padding uses xxl/sm from spacing map
</verification>

<success_criteria>
- Play screens aligned to layout spec
- menu.vue unchanged (documented exception)
</success_criteria>

<output>
After completion, create `.planning/phases/03-screen-design-ia/03-02-SUMMARY.md`
</output>
