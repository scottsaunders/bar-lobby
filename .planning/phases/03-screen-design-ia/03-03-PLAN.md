---
phase: 03-screen-design-ia
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/renderer/views/library/maps/maps.vue
  - src/renderer/views/library/maps/[id].vue
  - src/renderer/views/library/commands.vue
  - src/renderer/views/watch/replays.vue
autonomous: true
requirements:
  - UX-01
user_setup: []

must_haves:
  truths:
    - "Library and Watch screens use canonical view shell"
    - "commands.vue uses view-container (not commands-container)"
    - "view-container padding is consistent"
  artifacts:
    - path: src/renderer/views/library/commands.vue
      provides: "Commands view with view-container"
      contains: "view-container"
    - path: src/renderer/views/watch/replays.vue
      provides: "Replays view with canonical layout"
      contains: "view-container"
  key_links:
    - from: "library/commands.vue"
      to: "_views.scss"
      via: "class view-container"
      pattern: "view-container"
---

<objective>
Align Library (maps, maps/[id], commands) and Watch (replays) screens to the canonical view shell. Migrate commands.vue from commands-container to view-container; standardize view-container padding across all four screens.

Purpose: Library and Watch flows must have consistent layout with Play.
Output: Library + Watch screens conforming to layout spec.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-screen-design-ia/03-RESEARCH.md
@.planning/phases/03-screen-design-ia/03-01-PLAN.md
@src/renderer/styles/_views.scss

commands.vue currently uses .commands-container instead of .view-container — migrate to view-container for consistency. maps.vue and replays.vue already use view-container with canonical padding; remove redundant overrides where they match the base.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate commands.vue to view-container</name>
  <files>src/renderer/views/library/commands.vue</files>
  <action>
1. Replace div.commands-container with div.view-container in the template.
2. Rename or remove the scoped .commands-container block. If it only defines layout (flex, padding) that matches the base .view-container from _views.scss, remove it entirely. If it has commands-specific overrides, keep those under a different class (e.g. .commands-content) and ensure the root uses .view-container.
3. Ensure view-title is present with h1 (and optional p). commands.vue already has view-title with h1 — keep it.
4. Use canonical padding (0 xxl sm xxl) via the base class; add @use for spacing if needed for any overrides.
  </action>
  <verify>
    <automated>rg "view-container" src/renderer/views/library/commands.vue</automated>
  </verify>
  <done>commands.vue uses view-container; commands-container removed</done>
</task>

<task type="auto">
  <name>Task 2: Standardize view-container in maps, maps/[id], replays</name>
  <files>src/renderer/views/library/maps/maps.vue, src/renderer/views/library/maps/[id].vue, src/renderer/views/watch/replays.vue</files>
  <action>
For each file: Inspect the scoped .view-container block. If padding differs from canonical (0 xxl sm xxl), align to canonical. If the block only duplicates the base _views.scss .view-container (padding, flex, overflow, .view-title padding-left: 0), remove the duplicate parts — keep only screen-specific overrides (e.g. .maps-layout, .replays-layout, .map-preview-container). Use map.get($spacing, "xxl") and map.get($spacing, "sm") for any remaining padding. Ensure view-title (h1 + optional p) is present in each.
  </action>
  <verify>
    <automated>rg "view-container|view-title" src/renderer/views/library/maps/maps.vue src/renderer/views/watch/replays.vue</automated>
  </verify>
  <done>maps, maps/[id], replays use canonical view-container; redundant CSS removed</done>
</task>

</tasks>

<verification>
- commands.vue has view-container (not commands-container)
- maps.vue, maps/[id].vue, replays.vue have view-container with canonical padding
- All have view-title
</verification>

<success_criteria>
- Library and Watch screens aligned to layout spec
- Design system spacing used throughout
</success_criteria>

<output>
After completion, create `.planning/phases/03-screen-design-ia/03-03-SUMMARY.md`
</output>
